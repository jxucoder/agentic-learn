Implemented and ran a full scikit-learn solution with feature engineering in [solution.py](/Users/jiaruixu/work_space/agentic-learn/output/titanic/step_000/solution.py).

Key data exploration findings used:
- Shape: `1309 x 14`, target balance: `survived=1` is `500/1309`.
- Heavy missingness in `body`, `cabin`, `boat`, `home.dest`, moderate in `age`.
- Titles and cabin letters are informative enough to engineer.

What the script does:
- Drops leak columns: `boat`, `body`, `home.dest`.
- Engineers:
  - `title` from `name` (with rare-title grouping),
  - `family_size` and `is_alone`,
  - grouped `cabin_group` from cabin letters,
  - `fare_per_person`.
- Builds preprocessing + model pipelines.
- Evaluates multiple models with stratified 5-fold CV on `f1`, keeps best mean score.
- Prints exactly one stdout line as JSON and writes same JSON to `result.json`.

Run result:
- Stdout: `{"metric": 0.767414}`
- Saved file: [result.json](/Users/jiaruixu/work_space/agentic-learn/output/titanic/step_000/result.json) with `{"metric": 0.767414}`